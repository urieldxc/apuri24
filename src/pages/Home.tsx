import { Typography } from "@mui/material"
import { Navbar } from "../components/Navbar"

export const Home = () => {
  return (
    <div>
        <Navbar />
        <Typography variant="h3" component="h4">HOME</Typography>
    </div>
  )
}
